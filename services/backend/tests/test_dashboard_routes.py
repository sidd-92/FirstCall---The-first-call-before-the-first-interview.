"""Placeholder tests for authenticated dashboard routes.

TODO: once implemented, test GET /candidates, GET /candidates/{id},
POST /candidates/{id}/assign-screening, and POST /candidates/{id}/shortlist
with a mocked/valid Auth0 JWT (see auth.py's get_current_business).
"""


def test_placeholder() -> None:
    assert True
